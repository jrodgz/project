var extractor = extractor || {};

// namespace for reading the google spreadsheet
(function (memento, $, undefined) {

    // TODO, go through each memento, and grab the time of archive out..
    // http://wayback.archive-it.org/2358/20120116133100/http://www.bbc.co.uk/news/world/middle_east/

    // from what I can tell, we only need to get the first 8 didgits, after uri/id/timestamp value

}(window.extractor.memento = window.extractor.memento || {}, jQuery));